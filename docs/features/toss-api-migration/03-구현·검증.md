# 03-구현·검증 — 토스증권 API 전면 교체

> 누적 기록. 각 Phase 결과를 위에서부터 추가. 상세 설계는 `01-계획.md` 참조.

## 진행 현황 (Phase)
- [x] **Phase 1 — 인프라** (2026-06-24 완료)
- [x] **Phase 2 — 환율** (2026-06-24 완료)
- [x] **Phase 3 — 국내 현재가** (2026-06-24 완료)
- [x] **Phase 5 — 차트(개별종목)** (2026-06-24 완료, 버그수정 포함)
- [x] **Phase 7 — 종목 메타 토스화** (2026-06-24 완료, 마이그레이션 실행됨)
- [x] **Phase 4 — 미국 현재가** (2026-06-24 토스 전환·Yahoo 폴백·regular_close 수정. 주간거래/애프터는 **내일 실측**으로 사후 확인, KIS 키 완전삭제는 그 후)
- [x] **Phase 6 — KIS 완전 제거** (2026-06-24 완료): 코스피→Yahoo, 死코드 파일 3개 삭제, 미국 폴백 Yahoo화(getYahooChartData·getPreviousClose), 한국 거래일 판정 KIS→토스 `/market-calendar/KR`, StockController KIS 메서드 9개·getAccessToken 삭제, `env('KIS_*')` 참조 0. **앱 = 토스 + Yahoo 2개** (KIS 의존 0). 검증: 미국·국내·지수·환율·코스피 무손상, KIS grep 잔재 0, 209 테스트 통과, 8080 안정(라벨 KIS→현재가갱신). `.env`의 `KIS_*` 줄 삭제 권장(코드 미참조).

---

## 구현

### backend (주인: backend-engineer)

**Phase 1 — 인프라 (2026-06-24)**
- 신규 `app/Services/Toss/TossApiClient.php` — `POST /oauth2/token`(client_credentials) 토큰 발급, `Cache::remember('toss_access_token', 82800)`(23h) 캐싱, 공통 `get()` 래퍼, rate-limit 가드(그룹별 최소간격), 토큰/시크릿 로그 마스킹.
- 신규 `app/Services/Toss/TossSymbolMapper.php` — 심볼 정규화 단일 소유. 미국 티커 그대로 / 국내 `.KS`·`.KQ` 제거 / 신형코드(0167A0, `0167AO`오타교정) / 지수 skip(`NQ=F`,`^KS200`,`USDKRW=X`,`KOSPI_NIGHT`,`KOSPI200`→null). `market()`=KR/US/INDEX.
- 변경 `config/services.php` — `toss`(url·client_id·client_secret) 항목 추가, `.env`에서 로드.
- **기존 KIS/Yahoo 코드 무변경**(git 확인: config/services.php만 M, 나머지 신규).

**Phase 2 — 환율 (2026-06-24)**
- 신규 `app/Services/Toss/TossFxProvider.php` — `/api/v1/exchange-rate?baseCurrency=USD&quoteCurrency=KRW` 호출, `rate` 파싱, graceful null.
- 변경 `app/Services/FxService.php` — KIS 경로(`fetchFromKis`·`getKisToken`)·Yahoo 경로(`fetchFromYahoo`) **제거**, 1순위를 `TossFxProvider`로. **DB 캐시 폴백·신선도 유지**. `getUsdKrw()` 반환구조(`rate/recorded_at/source`) 무변경 → 호출부 무영향.
- 변경 `app/Providers/AppServiceProvider.php` — `TossApiClient`·`TossFxProvider` 싱글톤 등록, FxService 주입 전환.
- 검증: 토스 환율 `rate 1551.6 source Toss_ExchangeRate` OK / DB 폴백 `db_fallback 1540.0` OK / WS(`WebSocketAgentServer`)에서 FxService 미사용 → 8080 재시작 불필요.

**Phase 3 — 국내 현재가 (2026-06-24)**
- 신규 `app/Services/Toss/TossPriceFetcher.php` — `/api/v1/prices` 배치(≤195 청크), 국내만, 결과를 기존 캐시키 `kis_realtime_price_{ticker}`·폴백 `kis_last_successful_price_{ticker}`에 하위호환 채움.
- 신규 `app/Services/Toss/TossChangeCalculator.php` — `/candles?interval=1d&count=2` 직전봉 종가로 등락 계산, `toss_prev_close_{symbol}` KST 자정 TTL.
- 신규 `app/Services/Quote/TossQuoteProvider.php` — `QuoteProviderInterface` 구현(KR 전용), 기존 반환 키 동일.
- 변경 `PriceService`(resolveProvider KR→Toss), `AppServiceProvider`(토스 싱글톤·주입), `WebSocketAgentServer`(WS 사이클 국내=토스/미국=KIS 분리), `StockController::fetchDomesticPriceFromKis`(토스 위임).
- **미국·지수 무변경**(미국=KIS 유지, 지수 skip). 8080 재시작 완료 → `kis_realtime_price_005930` 토스값 전진 확인.

**Phase 5 — 개별종목 차트 (2026-06-24)**
- 신규 `app/Services/Toss/TossCandleProvider.php` — `/api/v1/candles`(1m/1d), `nextBefore` 페이지네이션(최대 5p), 1m→분봉 집계(OHLCV), 기존 `getYahooChartData` 포맷 정규화. 지수 skip.
- 변경 `StockController` — 국내(L283)·미국(L397) 개별종목 차트를 `TossCandleProvider`로, 실패 시 Yahoo 폴백. **지수 차트(NQ=F·코스피·야간선물) 무손상**(diff 0).
- 검증: 005930=`Toss (1d)`, AAPL=`Toss + KIS (현재가)`, NQ=F=`Yahoo Finance (1d)`(무손상).

**🔧 Phase 5 검증 중 발견·수정한 버그 3건 (2026-06-24)**
1. 🔴 **토스 토큰 401 무효화 시 자동복구 없음** → `TossApiClient::get`에 401 감지 시 토큰 forget+강제재발급+**1회 재시도** 추가. (원인: 토스는 client당 활성토큰 1개 — 외부에서 토큰 재발급 시 앱 토큰 무효화. **검증 probe가 토큰을 발급해 앱 토큰을 죽인 것이 1차 트리거** → 이후 probe 금지, 앱/tinker 경유로만 검증.)
2. 🟡 **미국 차트 source 라벨 오류** → `StockController` L515 `'Yahoo + KIS (Daytime)'` 하드코딩을, 베이스(토스/Yahoo) 반영 `'Toss + KIS (현재가)'`로. (데이터는 이미 토스였고 라벨만 오류였음.)
3. 🟢 **rate-limit 경로 매칭 오류** → `RATE_LIMIT_US` 옛 경로(`/securities/...`)를 실제(`/api/v1/prices`·`/candles` 등)로 교정.
- 401 자동복구 검증: 토큰 삭제 후 호출 시 자동 재발급되어 정상 응답 확인. 8080(WS) 메인에서 재기동.

**🔥 Phase 3 회귀 버그 — WS 8080 즉사 (2026-06-24, "새로고침하니 안뜸" 신고로 발견)**
- 증상: REST(개별종목·대시보드)는 정상인데 새로고침 시 **실시간 그리드·관심종목이 안 뜸**. 8080이 부팅 직후 죽음.
- 원인: Phase 3에서 `WebSocketAgentServer.php` L345에 `Log::debug('[WS] 토스 국내 갱신', ...)`를 추가하면서 **`use Illuminate\Support\Facades\Log;` import 누락**. `Class 'App\Console\Commands\Log' not found`(`\Error`)가 아래 `catch(\Exception)`에 안 잡혀 **WS 첫 사이클(3초)에 프로세스 즉사**.
- 수정: `WebSocketAgentServer.php`에 `use ...Facades\Log;` 1줄 추가. → 사이클 12종목 정상 반복 확인(크래시 없음). **교훈: 8080 재시작 후 "LISTENING"만 보지 말고 첫 사이클(3초+) 통과까지 확인.**

**Phase 7 — 종목 메타 토스화 (2026-06-24, 코드 완료·마이그레이션 대기)**
- 신규 `app/Services/Toss/TossStockMaster.php` — `/api/v1/stocks` 배치(≤195) 조회 → `{name, type(stock/etf), currency}` 캐시(`toss_stock_master_{sym}`, TTL 1일), graceful 폴백(=심볼).
- 변경 `Stock` 모델 — `name`/`type`/`currency` **accessor**(토스 캐시 우선 → DB컬럼 폴백 → 컬럼없으면 기본값: 심볼/'stock'/market유도). `currency`=KR→KRW·US→USD 유도.
- 변경 컨트롤러 — `DashboardController`(getInfoBatch 배치 워밍, N+1 방지)·`Watchlist`·`Portfolio`·`StockController`(차트 응답 name 4곳 `getName` 합류). 하드코딩 종목명 3곳·`KrStockResolver` name·seeder name 정리. **검색 JSON 마스터(`searchStocks`)·지수 무손상.**
- 신규 마이그레이션 `2026_06_24_000001_drop_name_type_currency_from_stocks.php` — `name`·`type`·`currency` drop(down 복원). **미실행**(메인 승인 후).
- 🔴 **검증 중 발견·수정한 버그(메인)**: `TossStockMaster::fetchFromToss`가 `/stocks` 응답을 `$data['stocks']`/`$data[0]`로 찾아 **항상 빈 결과**였음(국내 종목명은 DB컬럼 폴백으로 가려져 정상처럼 보였고, 미국 MU만 티커로 노출돼 발각). 실제 토스 응답은 `{"result":[...]}` → **`result` 키 우선 파싱**으로 수정.
- 검증(REST 8000): 종목명 토스 일원화 — dashboard·차트 모두 `MU=마이크론 테크놀로지`·`TSLA=테슬라`·`AAPL=애플`·`005930=삼성전자`·`0167A0=SOL AI반도체TOP2플러스(etf)`. 지수 `NQ=F=나스닥100 선물`(무손상). 검색 정상. 8080 새 코드로 안정 가동(첫 사이클 통과).

---

## 테스트 (주인: qa-tester)
- **Phase 1**: `tests/Unit/TossSymbolMapperTest.php` 30개 통과(매핑 10케이스 + 시장분류). 실키로 토큰 발급 OK(값 비노출, 길이 검증), 캐시 재사용 OK. 전체 스위트 165 통과 / 1 스킵(기존 시간의존 테스트, 무관) — **회귀 없음**.
- **Phase 2**: `tests/Unit/TossFxProviderTest.php` 10개 통과. 전체 스위트 **175 통과** / 1 스킵 / 0 실패 — 회귀 없음.
- **Phase 3**: `TossPriceFetcherTest`(14)·`TossChangeCalculatorTest`(10) 추가. 전체 **199 통과** / 1 스킵 / 0 실패. `BadTickClampTest` 생성자 인자 보정.
  - **등락 데이터 교차검증(main)**: 토스 005930 일봉(06-23=310,500)을 Yahoo(06-23=310,000)와 대조 → **일치**. 삼성 06-23 -12% 급락·06-24 반등은 **실제 시장 변동**이며 등락 로직(현재가−직전거래일 종가) 정확. 005935/000660/0167A0도 동일 검증.
  - ⚠️ **장중 잔여검증**(다음 거래일): 장 시작 직후 `/candles 1d`의 `[0]`봉(오늘) 생성 타이밍에 따라 `prevClose=[1]` 선택이 한 칸 밀릴 가능성 → 장중 실측 필요(QA).
- **Phase 5 + 버그수정**: `TossCandleProvider`(차트)·`TossApiClient`(401 재시도) 단위 테스트 추가. 전체 **214 통과** / 1 스킵 / 0 실패 — 회귀 없음.
- **Phase 7**: `TossStockMasterTest`(17) 추가. Unit 전체 통과 / 회귀 없음. (기존 `KrStockResolverDbTest` 계열 17건 실패는 Doctrine DBAL 미설치 환경문제로 Phase 7 이전부터 존재.)
- Pint: 프로젝트에 `laravel/pint` 미설치 상태(기존부터). 코드는 `pint.json` 규칙 수동 준수.

## 보안 (주인: security-auditor — 민감 기능만)
- (Phase 6 또는 미국 전환 직전 일괄 점검 예정) `TOSS_CLIENT_SECRET` .env-only·토큰 로깅 마스킹.

## 리뷰 (주인: code-reviewer)
**정밀 점검 (2026-06-24, code-reviewer + Explore + main)**
- ✅ **DB**: stocks 컬럼(name/type/currency) 삭제 확인, prices regular_close 존재, FK 고아 0(prices·portfolio), 미적용 마이그레이션 없음.
- 🔴 **H-1 (수정 완료)**: 마이그레이션 실행됨에도 `Stock` fillable·`KrStockResolver`·`searchStocks`·`ClassifyEtfStocks`가 삭제 컬럼 참조 → **새 국내종목 추가 시 `Unknown column 'name'` 에러**(실증). 수정: fillable=`symbol,market,exchange`만, KrStockResolver `buildAttributes`에서 name/type/currency 제거(resolveName/Type·KR_NAME_MAP·ETF_KEYWORDS 삭제), searchStocks name쿼리 제거, ClassifyEtfStocks 무력화. 검증: 새 KR 종목 create 성공·검색·종목명 정상, 223 테스트 통과.
- ✅ **regular_close**: 코드(`regularMarketPrice` 1순위)는 검증된 정답, docblock 모순만 정합화.
- 🟢 **KIS 死코드 맵(Explore)**: `KisParallelPriceFetcher`·`KisDomesticQuoteProvider`·`KisOverseasQuoteProvider`·`kisIndexRequest` **완전 미사용** → Step 2 삭제 안전. 단 `fetchFromKis`·`fetchMinuteFromKis`는 `getPreviousClose`·`getYahooChartData` 폴백에서 호출 → 미국 폴백에 KIS 잔존(Step 2 Yahoo化). WS `parallelFetcher`·AppServiceProvider KIS·`KIS_ACCOUNT_NO` 미사용.
- ⚠️ **후속(H-2/H-3)**: WS 미국 regular_close 캐시미스 시 Yahoo 동기호출로 사이클 지연(일시적, 워밍 후 안정) / 미국 등락 prevClose TTL이 KST자정(ET 권장). 운영 모니터링·후속.
